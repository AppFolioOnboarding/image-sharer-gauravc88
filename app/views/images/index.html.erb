<h2>Stored Images</h2>
<div class="card-columns">
  <% @images.each do |image| %>
    <div class="card">
        <%= link_to (image_tag image.url, class: 'card-img'), image %>
        <p class="card-text"><%= image.tag_list %></p>
    </div>
  <% end %>
</div>
